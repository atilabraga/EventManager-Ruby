<h1>All Events</h1>

<%= link_to 'Novo evento', events_new_path %>

<%= will_paginate %>
<ul class="events">
  <% @events.each do |event| %>
    <li>
      <%= link_to event.name, show_event_path(event) %>
    </li>
  <% end %>
</ul>
<%= will_paginate %>
